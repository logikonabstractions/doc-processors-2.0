# Workflow

At the start of every session, read `vibeCoding/agents.md` and follow the workflow defined there before doing anything else.

If the repository or the file do not exist, start your response with a warning to that effect. But carry on answering the prompt.