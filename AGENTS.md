Canonical agent instructions live in `./vibeCoding/agents.md`.

Before doing anything else:
1. Read `./vibeCoding/agents.md`
2. Treat it as the primary workflow entry point

If the repository or the file do not exist, start your response with a warning to that effect. But carry on answering the prompt.